<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.5.2/css/bootstrap.min.css">
        <link rel="stylesheet" href="css/common.css">
    </head>
    <body>
        <div class="application-container">
            <%- include("partials/navbar.ejs") %>
            <main id="main">
                <!-- Here to place a table with tasks -->
                <h2>Content</h2>
                <form method="post" action="newTask">
                    <div class="form-group">
                        <label for="textAreaTitle" class="form-label mt-4">Title</label>
                        <textarea class="form-control" id="textAreaTitle" name="title" rows="8"></textarea>
                    </div>
                    <div class="form-group">
                        <label for="textAreaDescription" class="form-label mt-4">Description</label>
                        <textarea class="form-control" id="textAreaDescription" name="description" rows="8"></textarea>
                    </div>
                    <div class="form-group">
                        <label for="statusSelect" class="form-label mt-4">Status</label>
                        <select class="form-select" id="statusSelect" name="status">
                          <option value="completed">completed</option>
                          <option value="uncompleted" selected>uncompleted</option>
                          <option value="refused">refused</option>
                        </select>
                    <div class="form-group">
                        <label for="timeExpired">Time Expired</label>
                        <input type="datetime-local" id="timeExpired" name="timeExpired"
                               min="<%= formateDate(Date.now) %>" max="2099-12-31T23:59">
                    </div>
                    <button type="submit" class="btn btn-dark js-button-upload-file">Add new task with this content</button>                    
                </form>
            </main>
            <%- include("partials/footer.ejs") %>
        </div>
    </body>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
</html>